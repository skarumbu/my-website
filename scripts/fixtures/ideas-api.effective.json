{
  "architecture": {
    "keyPoints": [
      "POST /ideas enforces project linkage by resolving project_id server-side via project name, rejecting unregistered projects",
      "PATCH /ideas/{id} endpoint supports machine key authentication alongside user authentication, enabling bot-managed updates to idea metadata",
      "POST /projects now supports machine key authentication alongside user authentication, enabling automated project creation",
      "Backward compatibility: existing entities with old 'repos' JSON list have their first entry promoted to the 'repo' field on read",
      "New 'bot_blocked_by' field added to support tracking cross-repo dependencies, integrated into the bot-managed fields alongside 'bot_status', 'bot_pr_url', and 'bot_error'"
    ],
    "overview": "HTTP-triggered Azure Functions backed by Table Storage. EasyAuth handles browser auth transparently at the Azure layer — no auth code in the application. A separate machine key enables automation (ideas-bot, external scripts) to write ideas and create projects without user credentials. Idea creation resolves project_id server-side by project name, ensuring all ideas are linked to registered projects."
  },
  "dataFlow": [
    {
      "color": "orange",
      "label": "Bot updates idea status",
      "sublines": [
        "Updates 'bot_status' field in the ideas table",
        "Adds 'bot_blocked_by' field for cross-repo dependency tracking"
      ]
    },
    {
      "color": "green",
      "label": "Create idea with project linkage",
      "sublines": [
        "Resolves project_id server-side via project name",
        "Rejects idea creation if no matching project is registered"
      ]
    }
  ],
  "description": "Azure Functions app that stores and serves feature ideas in Table Storage. Supports two auth modes: browser JWT tokens via EasyAuth for read access, and an X-Ideas-Key machine write-key for automated writes. Exposes a trigger endpoint for the ideas-bot.",
  "features": [
    "PATCH /api/ideas/{id} — updates idea metadata, including bot-managed fields like 'status', now supports both EasyAuth JWT authentication and X-Ideas-Key machine write-key for access",
    "POST /api/ideas — enforces project linkage by resolving project_id server-side via project name, rejecting unregistered projects",
    "POST /api/projects — now supports both EasyAuth JWT authentication and X-Ideas-Key machine write-key for project creation"
  ],
  "key": "ideas-api",
  "pipeline": [
    {
      "label": "git push\nmain"
    },
    {
      "color": "blue",
      "label": "GitHub Actions"
    },
    {
      "label": "func publish\n--python"
    },
    {
      "color": "green",
      "label": "Live on\nAzure Functions"
    }
  ],
  "relatedPages": [
    "ideas-bot",
    "authentication"
  ],
  "repoUrl": "https://github.com/skarumbu/ideas-api",
  "role": "Stores/serves AI-generated feature ideas; triggers ideas-bot",
  "runsOn": "Azure Functions",
  "summary": "Stores and serves AI-generated feature ideas and project data in Azure Table Storage. Enforces project linkage on idea creation, keyed by project name, and rejects unregistered projects. Accepts browser JWT tokens via EasyAuth for read access and an X-Ideas-Key machine write-key for automated writes, now extended to project creation.",
  "techStack": [
    "Azure Functions v2",
    "Python 3.11",
    "Azure Table Storage",
    "EasyAuth",
    "Managed Identity",
    "azure-containerapp"
  ],
  "title": "ideas-api",
  "updatedAt": "2026-08-29",
  "updatedBySha": "d61e3d5"
}
