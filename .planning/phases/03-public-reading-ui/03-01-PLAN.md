---
phase: 03-public-reading-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/Posts.tsx
  - src/styling/posts.css
  - src/index.js
  - src/__tests__/Posts.test.tsx
  - src/__tests__/PostReader.test.tsx
autonomous: true
requirements: [READ-01, READ-02]
must_haves:
  truths:
    - "Visiting /posts shows a list of published posts sorted newest first"
    - "Each list row shows the post title, formatted date, and description"
    - "Clicking a row navigates to /posts/:slug"
    - "When the API returns no posts, an empty-state message appears"
    - "When the env var is missing or the fetch fails, an inline error message appears"
  artifacts:
    - path: "src/Posts.tsx"
      provides: "/posts list page component with fetch + tri-state render"
      min_lines: 50
      contains: "REACT_APP_POSTS_API_BASE_URL"
    - path: "src/styling/posts.css"
      provides: "Vertical-list row layout styling (D-01)"
      contains: ".posts-row"
    - path: "src/__tests__/Posts.test.tsx"
      provides: "Failing-first unit tests for READ-01, READ-02"
      contains: "global.fetch"
    - path: "src/__tests__/PostReader.test.tsx"
      provides: "Wave 0 scaffold with react-markdown mock for Plan 02"
      contains: "jest.mock('react-markdown'"
  key_links:
    - from: "src/Posts.tsx"
      to: "REACT_APP_POSTS_API_BASE_URL/api/posts"
      via: "fetch in useEffect"
      pattern: "fetch\\(`\\$\\{BASE_URL\\}/api/posts`"
    - from: "src/index.js"
      to: "src/Posts.tsx"
      via: "createBrowserRouter route entry"
      pattern: "path: \"/posts\""
---

<objective>
Deliver the `/posts` list vertical slice: a public page that fetches published posts from the Phase 2 API and renders them as a vertical list of rows (title, date, description), each linking to its reader page. This plan also lays the Wave 0 test foundation for the whole phase — installing `react-markdown` and creating both test files (including the `PostReader.test.tsx` scaffold and its react-markdown mock) that Plan 02 will extend.

Purpose: First user-visible capability of Phase 3 — a reader can browse the writing feed (READ-01, READ-02).
Output: `react-markdown` installed; `src/Posts.tsx`, `src/styling/posts.css`, two test files; `/posts` route registered.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-public-reading-ui/03-CONTEXT.md
@.planning/phases/03-public-reading-ui/03-RESEARCH.md
@.planning/phases/03-public-reading-ui/03-PATTERNS.md
@.planning/phases/03-public-reading-ui/03-UI-SPEC.md
@.planning/phases/03-public-reading-ui/03-VALIDATION.md
@src/Ideas.tsx
@src/MomentumFinder.tsx
@src/components/Spinner.tsx
@src/index.js

<interfaces>
<!-- API response shape, confirmed from posts-api/function_app.py (RESEARCH.md). -->
<!-- Executor should use these directly — no codebase exploration needed. -->

GET /api/posts response:
  { posts: { title: string; slug: string; date: string; description: string; updatedAt: string }[] }

GET /api/posts/:slug response (used by Plan 02, mocked in the PostReader test scaffold here):
  { title: string; slug: string; date: string; description: string; updatedAt: string; body: string }

Error response (4xx/5xx): { error: string }

Env var: process.env.REACT_APP_POSTS_API_BASE_URL (string | undefined)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-markdown and create Wave 0 test scaffolds</name>
  <files>package.json, package-lock.json, src/__tests__/Posts.test.tsx, src/__tests__/PostReader.test.tsx</files>
  <read_first>
    - C:\Users\Sriram\my-website\package.json (confirm @testing-library/react ^13.4.0 present, react-markdown absent)
    - C:\Users\Sriram\my-website\.planning\phases\03-public-reading-ui\03-RESEARCH.md (Pitfall 2: react-markdown ESM mock; Package Legitimacy Audit: react-markdown approved [OK])
    - C:\Users\Sriram\my-website\.planning\phases\03-public-reading-ui\03-PATTERNS.md (Posts.test.tsx and PostReader.test.tsx structures, lines 580-652)
  </read_first>
  <action>
    Run `npm install react-markdown` (resolves to ^10.1.0 — approved [OK] in the Package Legitimacy Audit, postinstall: none). This adds it to package.json dependencies and updates package-lock.json.

    Create `src/__tests__/Posts.test.tsx` covering READ-01 and READ-02. Import React, render/screen/waitFor from @testing-library/react, MemoryRouter from react-router-dom, and Posts from '../Posts'. In beforeEach assign `global.fetch = jest.fn()`; in afterEach call `jest.resetAllMocks()`. Set `process.env.REACT_APP_POSTS_API_BASE_URL = 'http://test.local'` in beforeEach so the env guard passes. Write a test "renders post list rows with title, date, and description" that mocks fetch to resolve `{ ok: true, json: async () => ({ posts: [{ title: 'Hello World', slug: 'hello-world', date: '2026-05-30', description: 'A first post', updatedAt: '' }] }) }`, renders `<MemoryRouter><Posts /></MemoryRouter>`, and asserts via waitFor that 'Hello World' and 'A first post' are in the document and that an anchor with href '/posts/hello-world' exists (query by role 'link' and check getAttribute('href')). Write a second test "shows empty-state message when posts array is empty" mocking `{ ok: true, json: async () => ({ posts: [] }) }` and asserting the text 'No posts yet. Check back soon.' appears.

    Create `src/__tests__/PostReader.test.tsx` as the Wave 0 scaffold for Plan 02. It MUST begin (before importing PostReader) with the react-markdown ESM mock: `jest.mock('react-markdown', () => ({ __esModule: true, default: ({ children }) => <div data-testid="markdown-content">{children}</div> }))`. Import render/screen/waitFor, MemoryRouter/Routes/Route from react-router-dom, then `import PostReader from '../PostReader'`. Add the same global.fetch beforeEach/afterEach and env-var setup. Write one test "renders post title, back link, and markdown body" that mocks fetch to resolve `{ ok: true, json: async () => ({ title: 'My Post', slug: 'my-post', date: '2026-05-30', description: 'Desc', updatedAt: '', body: '# Hello' }) }`, renders PostReader inside `<MemoryRouter initialEntries={['/posts/my-post']}><Routes><Route path="/posts/:slug" element={<PostReader />} /></Routes></MemoryRouter>`, and asserts 'My Post' appears, the '← Writing' link exists with href '/posts', and the markdown mock received '# Hello' (query data-testid 'markdown-content'). This test WILL fail until Plan 02 creates `src/PostReader.tsx` — that is the intended RED state.
  </action>
  <verify>
    <automated>cd C:/Users/Sriram/my-website && node -e "require('./package.json').dependencies['react-markdown'] || process.exit(1)"</automated>
    <automated>cd C:/Users/Sriram/my-website && CI=true npx jest --testPathPattern="Posts.test" 2>&1 | grep -q "renders post list rows"</automated>
  </verify>
  <acceptance_criteria>
    - package.json dependencies contains a "react-markdown" entry
    - src/__tests__/Posts.test.tsx contains `global.fetch = jest.fn()`
    - src/__tests__/Posts.test.tsx asserts text 'No posts yet. Check back soon.'
    - src/__tests__/PostReader.test.tsx contains `jest.mock('react-markdown'` before the PostReader import
    - src/__tests__/PostReader.test.tsx contains `data-testid="markdown-content"` (or `data-testid='markdown-content'`)
    - `CI=true npx jest --testPathPattern="Posts.test"` reports the Posts test as the active spec (Posts.test runs; PostReader.test fails RED because PostReader.tsx does not exist yet — acceptable for this wave)
  </acceptance_criteria>
  <done>react-markdown is in package.json; both test files exist; Posts.test.tsx is written against the not-yet-built Posts component (RED); PostReader.test.tsx mock scaffold is in place for Plan 02.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Posts.tsx list page and posts.css styling</name>
  <files>src/Posts.tsx, src/styling/posts.css</files>
  <behavior>
    - Test 1 (READ-01): when fetch resolves with a posts array, each post renders as a `.posts-row` link to `/posts/:slug`
    - Test 2 (READ-02): each row shows title, formatted date, and description text
    - Test 3: empty array renders the `.posts-empty` "No posts yet. Check back soon." message
    - Test 4: missing BASE_URL or non-ok fetch sets `.posts-error` text
  </behavior>
  <read_first>
    - C:\Users\Sriram\my-website\src\Ideas.tsx (env-guard + fetch + error-state pattern)
    - C:\Users\Sriram\my-website\src\MomentumFinder.tsx (tri-state loading/error/data JSX structure)
    - C:\Users\Sriram\my-website\src\components\Spinner.tsx (no-prop spinner import)
    - C:\Users\Sriram\my-website\.planning\phases\03-public-reading-ui\03-PATTERNS.md (Posts.tsx full pattern, lines 28-124; posts.css full pattern, lines 222-341)
    - C:\Users\Sriram\my-website\.planning\phases\03-public-reading-ui\03-UI-SPEC.md (Posts.tsx component inventory, copywriting, typography, color)
    - C:\Users\Sriram\my-website\src\__tests__\Posts.test.tsx (the assertions this component must satisfy)
  </read_first>
  <action>
    Create `src/Posts.tsx` as a default-exported `Posts` function component. Imports in order: React + { useState, useEffect } from 'react'; NavBar from './components/nav-bar.tsx'; Spinner from './components/Spinner.tsx'; then `import './styling/posts.css'` LAST (CSS-import-last convention; explicit `.tsx` extensions on local imports). Declare `const BASE_URL = process.env.REACT_APP_POSTS_API_BASE_URL;` at module scope and a `Post` interface { title, slug, date, description, updatedAt: string }. Add a module-scope `fmtDate(iso: string): string` helper that splits `iso.slice(0,10)` on '-' into year/month/day Numbers and returns `new Date(year, month - 1, day).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })` — this avoids the UTC off-by-one (RESEARCH.md Pitfall 4). State: `posts: Post[]` (default []), `loading: boolean` (default false), `error: string | null` (default null). In useEffect with empty deps: if `!BASE_URL` call `setError('REACT_APP_POSTS_API_BASE_URL is not configured')` and return; else setLoading(true), setError(null), `fetch(`${BASE_URL}/api/posts`)`, throw on `!res.ok` with `${res.status} ${res.statusText}`, `setPosts(json.posts ?? [])`, catch sets error message, finally setLoading(false). Render `.posts-page` > `<NavBar />` > `.posts-content` containing `<h1 className="posts-heading">Writing</h1>` (D-02, D-03), then: `{loading && <Spinner />}`, `{error && <p className="posts-error">{error}</p>}`, empty-state `{!loading && !error && posts.length === 0 && <p className="posts-empty">No posts yet. Check back soon.</p>}`, and the list `{!loading && !error && posts.length > 0 && <ul className="posts-list">...}`. Each item: `<li key={post.slug}><a className="posts-row" href={`/posts/${post.slug}`}><span className="posts-row-title">{post.title}</span><span className="posts-row-date">{fmtDate(post.date)}</span><span className="posts-row-desc">{post.description}</span></a></li>`. Do NOT sort client-side beyond trusting API order, but the API already returns newest-first; render in received order (READ-01). Do NOT use dangerouslySetInnerHTML anywhere.

    Create `src/styling/posts.css` per 03-PATTERNS.md lines 222-341 and the UI-SPEC. Start with the Google Fonts `@import` (Fraunces + DM Sans, omit Caprasimo) as the first line. Do NOT redeclare `:root` — reuse the global tokens (`--bg-1`, `--ink`, `--ink-2`, `--ink-3`, `--line`, `--accent`). Implement `.posts-page` (min-height 100vh, background var(--bg-1), DM Sans, color var(--ink)); `.posts-content` (max-width 720px, margin 0 auto, padding 24px 40px 120px); `.posts-heading` (Fraunces 48px/400, line-height 0.95, letter-spacing -0.03em, margin 48px 0 32px); `.posts-list` (list-style none, margin 0, padding 0); `.posts-row` as CSS Grid `grid-template-columns: 1fr auto`, two rows, padding 20px 0, border-bottom 1px solid var(--line), text-decoration none, color inherit, border-radius 8px, transition background 180ms + padding-left 180ms, cursor pointer; `.posts-row:hover` background rgba(255,255,255,0.6) + padding-left 12px; `.posts-row-title` (Fraunces 22px/600, grid-column 1 row 1, color var(--ink)); `.posts-row-date` (DM Sans 13px, color var(--ink-3), white-space nowrap, grid-column 2 row 1, align-self center, padding-left 16px); `.posts-row-desc` (DM Sans 16px, color var(--ink-2), line-height 1.5, margin-top 4px, grid-column 1 / -1, row 2); `.posts-error` (font-size 13px, color var(--accent), margin 0 0 12px); `.posts-empty` (font-size 16px, color var(--ink-2), text-align center, padding 48px 0).
  </action>
  <verify>
    <automated>cd C:/Users/Sriram/my-website && CI=true npx jest --testPathPattern="Posts.test"</automated>
    <automated>cd C:/Users/Sriram/my-website && grep -L "dangerouslySetInnerHTML" src/Posts.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/Posts.tsx contains `const BASE_URL = process.env.REACT_APP_POSTS_API_BASE_URL`
    - src/Posts.tsx contains the literal `fetch(`${BASE_URL}/api/posts`)`
    - src/Posts.tsx contains `<h1 className="posts-heading">Writing</h1>`
    - src/Posts.tsx contains `href={`/posts/${post.slug}`}`
    - src/Posts.tsx does NOT contain the string `dangerouslySetInnerHTML`
    - src/styling/posts.css contains `.posts-row` with `display: grid`
    - `CI=true npx jest --testPathPattern="Posts.test"` exits 0 (all Posts tests green)
  </acceptance_criteria>
  <done>The /posts list renders rows with title/date/description from API data, handles empty and error states, and all Posts.test.tsx assertions pass green.</done>
</task>

<task type="auto">
  <name>Task 3: Register /posts route in index.js</name>
  <files>src/index.js</files>
  <read_first>
    - C:\Users\Sriram\my-website\src\index.js (existing createBrowserRouter array, lines 20-53; import block lines 1-16)
    - C:\Users\Sriram\my-website\.planning\phases\03-public-reading-ui\03-PATTERNS.md (index.js edit pattern, lines 556-576)
  </read_first>
  <action>
    Edit `src/index.js`. Add `import Posts from './Posts.tsx';` to the existing import block (after the LearningPlan import on line 11, matching the explicit-`.tsx` convention). Add one route object to the `createBrowserRouter([...])` array after the `/learning-plan` entry: `{ path: "/posts", element: <Posts /> }`. Do NOT add the `/posts/:slug` route here — Plan 02 adds the PostReader import and that route. Do NOT modify the MSAL setup (lines 18, 57-67) — /posts is a public route. Leave a trailing-comma-safe array.
  </action>
  <verify>
    <automated>cd C:/Users/Sriram/my-website && grep -q 'path: "/posts"' src/index.js && grep -q "import Posts from './Posts.tsx'" src/index.js</automated>
    <automated>cd C:/Users/Sriram/my-website && CI=true npx jest --testPathPattern="Posts.test"</automated>
  </verify>
  <acceptance_criteria>
    - src/index.js contains `import Posts from './Posts.tsx'`
    - src/index.js contains `path: "/posts"` with `element: <Posts />`
    - src/index.js does NOT contain `PostReader` (that is Plan 02's concern)
    - The MSAL `msalInstance.initialize()` block is unchanged (still present in file)
  </acceptance_criteria>
  <done>The /posts route resolves to the Posts component; the dev server (and production build) can navigate to /posts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Posts API | Client fetches `REACT_APP_POSTS_API_BASE_URL/api/posts`; API response (title/date/description strings) is rendered into the DOM as React text children |
| npm registry → build | `npm install react-markdown` pulls third-party code into the bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering (XSS) | Posts.tsx rendering API strings (title/date/description) | mitigate | Render all API fields as JSX text children — React escapes them by default. No `dangerouslySetInnerHTML` (grep gate in Task 2 verify). |
| T-03-02 | Information Disclosure | `REACT_APP_POSTS_API_BASE_URL` in client bundle | accept | Value is a public API URL, not a secret; posts API requires no auth for reads (SEC-03 keeps blob private behind Functions). Low risk. |
| T-03-03 | Denial of Service | Unbounded posts list render | accept | Post count is low (<50 per STATE.md); no pagination needed per Out-of-Scope. Reassess if volume grows. |
| T-03-SC | Tampering | `npm install react-markdown` supply chain | mitigate | react-markdown is [OK]-approved in RESEARCH.md Package Legitimacy Audit (11-yr-old remarkjs library, postinstall: none). No new [ASSUMED]/[SUS] packages, so no blocking checkpoint required. |
</threat_model>

<verification>
- `CI=true npx jest --testPathPattern="Posts.test"` exits 0 (Posts list tests green).
- `grep dangerouslySetInnerHTML src/Posts.tsx` returns no match (SEC-01 foundation).
- `react-markdown` present in package.json (enables Plan 02).
- `/posts` route registered in src/index.js.
</verification>

<success_criteria>
- Navigating to `/posts` renders a vertical list of posts (title, date, description) sorted newest-first from the API (READ-01, READ-02).
- Each row links to `/posts/:slug`.
- Empty and error states display their designed messages.
- No `dangerouslySetInnerHTML` in Posts.tsx.
- Wave 0 test scaffolds for both pages exist; Posts tests are green.
</success_criteria>

<output>
Create `.planning/phases/03-public-reading-ui/03-01-SUMMARY.md` when done.
</output>
