---
phase: 03-public-reading-ui
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - src/components/nav-bar.tsx
  - src/components/DigitsNavBar.tsx
  - src/App.tsx
  - src/styling/home.css
  - .github/workflows/azure-static-web-apps.yml
autonomous: false
requirements: [READ-01]
user_setup:
  - service: github-actions
    why: "Posts API base URL must be available to the production build"
    env_vars:
      - name: REACT_APP_POSTS_API_BASE_URL
        source: "GitHub repo → Settings → Secrets and variables → Actions → New repository secret (value = deployed posts-api Function App base URL, e.g. https://posts-api-prod-xxxx.azurewebsites.net)"
must_haves:
  truths:
    - "A 'Writing' link appears in both nav bars and links to /posts"
    - "A 'Writing' card appears in the home page projects grid and links to /posts"
    - "The production build receives REACT_APP_POSTS_API_BASE_URL"
    - "A human confirms the live read path (list + reader + back link + markdown) works"
  artifacts:
    - path: "src/components/nav-bar.tsx"
      provides: "Writing nav link"
      contains: "/posts"
    - path: "src/components/DigitsNavBar.tsx"
      provides: "Writing entry in NAV_LINKS"
      contains: "Writing"
    - path: "src/App.tsx"
      provides: "Writing home card + nav link"
      contains: "card-writing"
    - path: ".github/workflows/azure-static-web-apps.yml"
      provides: "REACT_APP_POSTS_API_BASE_URL injected into build"
      contains: "REACT_APP_POSTS_API_BASE_URL"
  key_links:
    - from: "src/App.tsx"
      to: "/posts"
      via: "card anchor href"
      pattern: "href=\"/posts\""
    - from: ".github/workflows/azure-static-web-apps.yml"
      to: "build env"
      via: "env var from secret"
      pattern: "REACT_APP_POSTS_API_BASE_URL: \\$\\{\\{ secrets"
---

<objective>
Wire the now-working `/posts` and `/posts/:slug` pages into the site so users can discover them: add a "Writing" link to both nav bars, add a "Writing" card to the home page projects grid, and inject `REACT_APP_POSTS_API_BASE_URL` into the production build. Finish with a human-verify checkpoint confirming the live read path works end to end.

Purpose: Make the reading feature reachable from normal navigation and deployable to production.
Output: Updated nav bars, home card + CSS, CI workflow env var; verified live read path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-public-reading-ui/03-CONTEXT.md
@.planning/phases/03-public-reading-ui/03-PATTERNS.md
@.planning/phases/03-public-reading-ui/03-UI-SPEC.md
@src/components/nav-bar.tsx
@src/components/DigitsNavBar.tsx
@src/App.tsx
@.github/workflows/azure-static-web-apps.yml
@.planning/phases/03-public-reading-ui/03-02-SUMMARY.md

<interfaces>
<!-- Existing nav/card patterns to replicate exactly. -->
nav-bar.tsx: a series of `<a href="..." className="button">Label</a>` — last is Learning Plan.
DigitsNavBar.tsx: a `NAV_LINKS` array of `{ href, label }` objects, rendered by a .map loop.
App.tsx home nav: `.home-nav-links` div with `<a href="...">Label</a>` entries; projects grid uses
  `<a href="..." className="card card-X">` with .card-decor, .card-label, .card-title, .card-desc, .arrow.
home.css tokens available: --c-peach (#FCD9A6), --c-peach-ink (#8A5320); .card-trail is the span-3 analog.
azure-static-web-apps.yml: build step `env:` block already lists REACT_APP_* secrets; CI: false is present.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add "Writing" nav links to both nav bars</name>
  <files>src/components/nav-bar.tsx, src/components/DigitsNavBar.tsx</files>
  <read_first>
    - C:\Users\Sriram\my-website\src\components\nav-bar.tsx (existing `<a className="button">` link list)
    - C:\Users\Sriram\my-website\src\components\DigitsNavBar.tsx (NAV_LINKS array, lines 4-10)
    - C:\Users\Sriram\my-website\.planning\phases\03-public-reading-ui\03-PATTERNS.md (nav-bar edit lines 470-486; DigitsNavBar edit lines 490-515)
  </read_first>
  <action>
    In `src/components/nav-bar.tsx`, add one anchor after the Learning Plan link, matching the existing pattern exactly: `<a href="/posts" className="button">Writing</a>`. Use plain `<a href>` — do NOT introduce react-router `<Link>` or any Router import (the nav bar uses plain anchors throughout; D-09).

    In `src/components/DigitsNavBar.tsx`, append one object to the `NAV_LINKS` array after the learning-plan entry: `{ href: '/posts', label: 'Writing' }`. The existing `.map` render loop handles it automatically — no other change.
  </action>
  <verify>
    <automated>cd C:/Users/Sriram/my-website && grep -q 'href="/posts" className="button"' src/components/nav-bar.tsx</automated>
    <automated>cd C:/Users/Sriram/my-website && grep -q "label: 'Writing'" src/components/DigitsNavBar.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/nav-bar.tsx contains `<a href="/posts" className="button">Writing</a>`
    - src/components/nav-bar.tsx does NOT import from 'react-router-dom'
    - src/components/DigitsNavBar.tsx NAV_LINKS contains `{ href: '/posts', label: 'Writing' }`
  </acceptance_criteria>
  <done>Both nav bars show a "Writing" link pointing to /posts.</done>
</task>

<task type="auto">
  <name>Task 2: Add Writing home card + CSS and CI env var</name>
  <files>src/App.tsx, src/styling/home.css, .github/workflows/azure-static-web-apps.yml</files>
  <read_first>
    - C:\Users\Sriram\my-website\src\App.tsx (home nav links lines 17-24; card grid lines 32-102; card-trail lines 67-76)
    - C:\Users\Sriram\my-website\src\styling\home.css (`.card-trail` lines 213-218; `--c-peach`/`--c-peach-ink` tokens lines 19/27; responsive block ~lines 391-392)
    - C:\Users\Sriram\my-website\.github\workflows\azure-static-web-apps.yml (build step env block, lines 33-43)
    - C:\Users\Sriram\my-website\.planning\phases\03-public-reading-ui\03-PATTERNS.md (App.tsx card + CSS edits lines 519-552)
    - C:\Users\Sriram\my-website\.planning\phases\03-public-reading-ui\03-UI-SPEC.md (home card spec lines 205-215)
  </read_first>
  <action>
    In `src/App.tsx`: (a) add `<a href="/posts">Writing</a>` to the `.home-nav-links` div after the Learning Plan link. (b) add a new card to the `.projects` grid after the Learning Plan card (lines 91-100), copying the `.card-trail` structure: `<a href="/posts" className="card card-writing">` containing `<span className="card-decor" style={{ right: 12, bottom: -8, fontSize: 110 }}>✍</span>`, then a `<div>` with `<div className="card-label">Journal</div>`, `<div className="card-title">Writing</div>`, `<div className="card-desc">Thoughts, notes, and project write-ups.</div>`, then `<div className="arrow"><ArrowIcon /></div>`. Use the existing copy verbatim from UI-SPEC.

    In `src/styling/home.css`: add a `.card-writing` rule (copy `.card-trail` as template): `grid-column: span 3; background: var(--c-peach); color: var(--c-peach-ink);` plus `.card-writing .card-decor { right: 12px; bottom: -8px; font-size: 110px; }`. Also add `.card-writing` to the existing responsive `@media` selector list near lines 391-392 (alongside `.card-trail, .card-ideas`) so it collapses to full width on narrow screens like its siblings. Grid balance: the existing grid is `card-digits`(featured) + nba + trail(span 3) + ideas(span 2) + learn(span 4); adding `card-writing`(span 3) follows learn. Verify in the checkpoint (Task 3) that no orphaned partial row looks broken; if it does, the checkpoint will request a span adjustment (span 3 pairs with an adjacent span 3) — do not pre-optimize, ship span 3 per UI-SPEC.

    In `.github/workflows/azure-static-web-apps.yml`: add one line to the build step `env:` block (after the existing REACT_APP_* entries): `REACT_APP_POSTS_API_BASE_URL: ${{ secrets.REACT_APP_POSTS_API_BASE_URL }}`. Match the exact indentation of the surrounding env entries (10 spaces). Do NOT change `CI: false` or any other env var.
  </action>
  <verify>
    <automated>cd C:/Users/Sriram/my-website && grep -q 'card card-writing' src/App.tsx</automated>
    <automated>cd C:/Users/Sriram/my-website && grep -q ".card-writing" src/styling/home.css</automated>
    <automated>cd C:/Users/Sriram/my-website && grep -q "REACT_APP_POSTS_API_BASE_URL: " .github/workflows/azure-static-web-apps.yml</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsx contains `<a href="/posts" className="card card-writing">`
    - src/App.tsx home-nav-links contains `<a href="/posts">Writing</a>`
    - src/App.tsx contains the card copy `Thoughts, notes, and project write-ups.`
    - src/styling/home.css contains `.card-writing` with `grid-column: span 3` and `background: var(--c-peach)`
    - .github/workflows/azure-static-web-apps.yml contains `REACT_APP_POSTS_API_BASE_URL: ${{ secrets.REACT_APP_POSTS_API_BASE_URL }}`
    - .github/workflows/azure-static-web-apps.yml still contains `CI: false`
  </acceptance_criteria>
  <done>The home page links to Writing via both a nav link and a card; the CI build injects the posts API URL.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the live read path</name>
  <files>none (verification only — no files modified)</files>
  <what-built>
    The full Phase 3 read path is now wired: /posts list, /posts/:slug reader with markdown, "← Writing" back link, "Writing" links in both nav bars and a Writing card on the home page, and the CI env var.
  </what-built>
  <read_first>
    - C:\Users\Sriram\my-website\.planning\phases\03-public-reading-ui\03-VALIDATION.md (Manual-Only Verifications table — markdown visual render, back-link nav, CORS)
    - C:\Users\Sriram\my-website\.planning\phases\03-public-reading-ui\03-UI-SPEC.md (expected visuals for cross-check)
  </read_first>
  <action>
    Pause for human verification — this task modifies no files. Before signalling the human, ensure the dev server can reach the posts API: create `C:\Users\Sriram\my-website\.env.local` containing `REACT_APP_POSTS_API_BASE_URL=<deployed posts-api base URL>` (the Phase 2 Function App URL), then start `npm start`. Present the how-to-verify steps below to the human and wait for the resume signal. Do not proceed to writing the SUMMARY until the human types "approved".
  </action>
  <how-to-verify>
    1. Ensure the posts API base URL is available to the dev server via `C:\Users\Sriram\my-website\.env.local` with a single line: `REACT_APP_POSTS_API_BASE_URL=<deployed posts-api base URL>`. Note the Phase 2 API CORS allows `https://www.quixotry.me`; if `localhost:3000` is blocked (browser console shows an Access-Control-Allow-Origin error — RESEARCH.md Pitfall 3), either add `http://localhost:3000` to the dev function CORS config or verify against the deployed site after this branch is merged. State which path you used.
    2. Run `npm start` and open `http://localhost:3000/`.
    3. Confirm the home page shows a "Writing" card (peach background, ✍ decor) and a "Writing" nav link. Click the card → lands on `/posts`.
    4. On `/posts`: confirm the "Writing" H1 heading and a vertical list of rows, each showing title (left), date (right), description (below). Confirm rows are newest-first. Confirm hover gives the subtle background + left-pad shift.
    5. Click a row → lands on `/posts/:slug`. Confirm: "← Writing" back link at top, then title + date + description, then the markdown body. Verify the body renders headings, bold, italic, a link, a list, and a code block (monospace, darker background, NOT syntax-highlighted) — use a post that contains these, or note which elements were present.
    6. Click "← Writing" → returns to `/posts`.
    7. Confirm the reading column is comfortably narrow (~68ch) and centered on a wide window.
    8. Grid sanity: confirm the home projects grid rows look complete (no awkward orphaned half-row from the new span-3 Writing card). If it looks broken, report it so the span can be adjusted.
  </how-to-verify>
  <verify>
    <human-check>Human steps through the how-to-verify checklist and confirms the live read path works.</human-check>
  </verify>
  <acceptance_criteria>
    - Human confirms list renders newest-first with title/date/description (READ-01, READ-02)
    - Human confirms clicking a row opens the full article (READ-03)
    - Human confirms markdown elements render correctly (READ-04)
    - Human confirms "← Writing" returns to /posts (READ-05)
    - Human confirms home card + nav links navigate to /posts
    - Human confirms grid layout is not visually broken (or reports a needed span fix)
  </acceptance_criteria>
  <resume-signal>Type "approved" if the read path works, or describe the issues (e.g., CORS blocked, grid orphaned row, markdown element missing).</resume-signal>
  <done>Human has typed "approved" confirming the live read path (list → reader → back link, with markdown rendering) works end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub secret → build env | `REACT_APP_POSTS_API_BASE_URL` secret is injected into the client bundle at build time |
| Static markup → users | Nav links and home card are static anchors to a public route |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Information Disclosure | `REACT_APP_POSTS_API_BASE_URL` baked into the public bundle | accept | It is a public API base URL, not a secret credential; the posts API serves only published, public content (reads are unauthenticated by design). Same risk profile as the existing REACT_APP_* URLs already in the workflow. |
| T-03-09 | Tampering | static nav/card anchors | accept | Plain `<a href="/posts">` to a same-origin public route; no user input, no injection surface. |
</threat_model>

<verification>
- `grep "Writing" src/components/nav-bar.tsx src/components/DigitsNavBar.tsx` shows the link in both.
- `grep "card-writing" src/App.tsx src/styling/home.css` shows card markup + CSS.
- `grep "REACT_APP_POSTS_API_BASE_URL" .github/workflows/azure-static-web-apps.yml` shows the build env var.
- Human checkpoint approved.
</verification>

<success_criteria>
- "Writing" link present in both nav bars and the home nav, all linking to /posts.
- "Writing" card present in the home projects grid linking to /posts.
- Production build receives REACT_APP_POSTS_API_BASE_URL.
- Live read path (list → reader → back link, with markdown rendering) verified by a human.
</success_criteria>

<output>
Create `.planning/phases/03-public-reading-ui/03-03-SUMMARY.md` when done.
</output>
