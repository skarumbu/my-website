---
phase: 02-public-reading-api
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified: []
autonomous: false
requirements:
  - SEC-03

must_haves:
  truths:
    - "A direct HTTP request to the posts container bypassing Azure Functions returns 403 Forbidden"
    - "The Azure Functions endpoints introduced in plan 02-01 remain reachable (returns 200 from GET /api/posts)"
  artifacts:
    - path: "C:\\Users\\Sriram\\azure-infrastructure\\modules\\postsapi.bicep"
      provides: "Container privacy settings already deployed"
      contains: "allowBlobPublicAccess: false, publicAccess: 'None'"
  key_links:
    - from: "Browser"
      to: "Azure Blob Storage (posts container)"
      via: "Direct HTTPS URL — must return 403"
      pattern: "curl -I https://{account}.blob.core.windows.net/posts/"
---

<objective>
Verify SEC-03: confirm the Azure Blob Storage "posts" container is private and that direct browser/curl requests to blob URLs return HTTP 403 (not 200 or 401). No code changes are needed — this container privacy was provisioned in Phase 1 Bicep. This plan is a smoke-test checkpoint only.

Purpose: Prove the trust boundary between public internet and blob storage is enforced. Phase 3 UI depends on the promise that post content is only accessible through Azure Functions.

Output: Human confirmation that direct blob access is rejected with 403.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\Sriram\my-website\.planning\ROADMAP.md
@C:\Users\Sriram\my-website\.planning\phases\02-public-reading-api\02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Retrieve storage account name and construct direct blob URL for smoke test</name>
  <files></files>

  <read_first>
    - C:\Users\Sriram\azure-infrastructure\modules\postsapi.bicep — locate the storageAccount resource to find the name prefix or parameterized name
    - C:\Users\Sriram\posts-api\local.settings.json (if it exists) — may contain POSTS_STORAGE_ACCOUNT_NAME already
  </read_first>

  <action>
    Determine the deployed storage account name by running:
      az storage account list --query "[?contains(name,'posts')].name" --output tsv

    If the az CLI is not authenticated, run: az login

    Once the account name is known (e.g., "postsstorage12345"), construct the direct blob URL:
      https://{account_name}.blob.core.windows.net/posts/

    Run a curl probe against the container root (no SAS token, no auth header):
      curl -I "https://{account_name}.blob.core.windows.net/posts/" --max-time 10

    Expected: HTTP/1.1 403 (or 400 PublicAccessNotPermitted — both indicate the container is private).
    Not expected: 200, 401 (401 would mean auth is checked but public is allowed).

    Also confirm the Azure Functions health endpoint still responds:
      curl -s "https://{functions_host}/api/health"

    Where {functions_host} is the Azure Static Web Apps linked Functions host or the direct Functions URL from the Azure portal. If the host URL is unknown, retrieve it with:
      az functionapp list --query "[?contains(name,'posts')].{name:name,defaultHostName:defaultHostName}" --output table

    Output both results (the 403 curl and the health 200) as text — the checkpoint task will present them to the user for verification.
  </action>

  <verify>
    <automated>az storage account list --query "[?contains(name,'posts')].name" --output tsv 2>&1</automated>
  </verify>

  <acceptance_criteria>
    - Storage account name is retrieved successfully from az CLI output
    - curl against the direct blob container URL produces an HTTP response (not a network error)
    - The HTTP status from the direct blob URL is 403 or 400 — any 2xx response means the container is misconfigured and must be fixed before proceeding
    - Azure Functions health endpoint returns 200 (confirms the Functions app is live and the SEC-03 fix has not broken anything)
  </acceptance_criteria>

  <done>Direct blob URL response status and Functions health status are both known and ready for human verification</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify SEC-03 — container is private, direct access returns 403</name>

  <read_first>
    - Output from Task 1 (curl results for both the direct blob URL and the health endpoint)
  </read_first>

  <what-built>
    Task 1 ran curl against the direct Azure Blob Storage container URL (no SAS token, no auth header) and retrieved the HTTP status code. SEC-03 requires this to return 403 or 400 — not 200.
  </what-built>

  <how-to-verify>
    Review the curl output from Task 1:

    1. Confirm the direct blob URL response is HTTP 403 (or 400 with error code PublicAccessNotPermitted).
       The response header should look like: HTTP/1.1 403 Server failed to authenticate the request
       or: HTTP/1.1 400 Public access is not permitted on this storage account.

    2. Confirm the Azure Functions health endpoint returned 200 with body {"status": "ok", "service": "posts-api"}.

    3. Optionally, open a browser and navigate to https://{account}.blob.core.windows.net/posts/
       — the browser should show an XML error response (403 or 400), not a file listing.

    If the direct blob URL returns 200: the container is misconfigured. Open postsapi.bicep, confirm allowBlobPublicAccess: false and publicAccess: 'None' are set, re-deploy, and re-run the curl check.
  </how-to-verify>

  <resume-signal>Type "confirmed" if direct blob access returns 403/400 and health returns 200. Or describe the actual HTTP status if it differs.</resume-signal>

  <acceptance_criteria>
    - User confirms direct blob URL returns 403 or 400 (not 200)
    - User confirms Azure Functions /api/health returns 200
    - SEC-03 requirement is satisfied: blob storage is structurally inaccessible from the public internet without going through Azure Functions
  </acceptance_criteria>

  <done>SEC-03 verified: container is private, direct access rejected, Functions app is live</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Azure Blob Storage (direct) | Must be structurally blocked by container privacy settings |
| Internet → Azure Functions → Blob Storage | The only valid access path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Elevation of Privilege | Azure Blob Storage "posts" container | mitigate | Verify `allowBlobPublicAccess: false` + `publicAccess: 'None'` enforced in deployed infrastructure; smoke-test confirms 403 on direct access |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan |
</threat_model>

<verification>
This plan's verification is the checkpoint itself. After the human confirms 403, SEC-03 is complete.

If the check fails (200 returned):
1. Read C:\Users\Sriram\azure-infrastructure\modules\postsapi.bicep
2. Confirm allowBlobPublicAccess: false on storage account resource
3. Confirm publicAccess: 'None' on container resource
4. Re-deploy: az deployment group create ...
5. Re-run curl check
</verification>

<success_criteria>
- curl against direct blob URL returns 403 or 400 (human confirmed)
- Azure Functions /api/health returns 200 (confirms live deployment is unaffected)
- SEC-03 checkbox can be marked complete in REQUIREMENTS.md
</success_criteria>

<output>
Create C:\Users\Sriram\my-website\.planning\phases\02-public-reading-api\02-02-SUMMARY.md when done.
</output>
