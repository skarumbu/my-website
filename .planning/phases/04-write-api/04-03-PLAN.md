---
phase: 04-write-api
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - C:\Users\Sriram\azure-infrastructure\modules\postsapi.bicep
  - C:\Users\Sriram\azure-infrastructure\main.bicep
autonomous: false
requirements:
  - SEC-02

user_setup:
  - service: azure-ad
    why: "Easy Auth on posts-api Function App requires an App Registration with client ID and secret"
    env_vars:
      - name: postsApiClientId
        source: "Azure Portal → Microsoft Entra ID → App registrations → New registration → note Application (client) ID"
      - name: postsApiClientSecret
        source: "Azure Portal → App registration → Certificates & secrets → New client secret → note Value (only shown once)"
      - name: azureTenantId
        source: "Azure Portal → Microsoft Entra ID → Overview → Directory (tenant) ID (already used for ideas-api)"
    dashboard_config:
      - task: "Create App Registration for posts-api"
        location: "Azure Portal → Microsoft Entra ID → App registrations → New registration"
        details: "Name: posts-api. Supported account types: Accounts in this organizational directory only. No redirect URI needed. After creation, go to Expose an API → Add a scope → accept the default Application ID URI (api://<clientId>)."
      - task: "Create client secret"
        location: "Azure Portal → App registration (posts-api) → Certificates & secrets → Client secrets → New client secret"
        details: "Set an expiry (e.g., 24 months). Copy the Value immediately — it is only shown once."

must_haves:
  truths:
    - "Easy Auth authsettingsV2 resource exists in postsapi.bicep with requireAuthentication: false and AllowAnonymous"
    - "postsapi.bicep receives azureTenantId, postsApiClientId, postsApiClientSecret as parameters"
    - "main.bicep passes the three new params to the postsapi module call"
    - "After Bicep deploy, GET /api/posts returns 200 (public route still works)"
    - "After Bicep deploy, POST /api/posts with no Bearer token returns 401"
  artifacts:
    - path: "C:\\Users\\Sriram\\azure-infrastructure\\modules\\postsapi.bicep"
      provides: "authsettingsV2 resource + three new params"
      contains: "authsettingsV2"
    - path: "C:\\Users\\Sriram\\azure-infrastructure\\main.bicep"
      provides: "postsapi module call with new params wired"
      contains: "postsApiClientId"
  key_links:
    - from: "postsapi.bicep::functionApp appSettings"
      to: "POSTS_CLIENT_SECRET app setting"
      via: "postsApiClientSecret param passed as app setting value"
      pattern: "POSTS_CLIENT_SECRET"
    - from: "postsapi.bicep::authsettingsV2"
      to: "functionApp resource"
      via: "parent: functionApp"
      pattern: "parent: functionApp"
---

<objective>
Deliver the infrastructure slice: enable Azure App Service Easy Auth on the posts-api
Function App so that the X-MS-CLIENT-PRINCIPAL header is injected for authenticated requests.
Public GET routes remain unblocked (requireAuthentication: false + AllowAnonymous).

Purpose: Without this Bicep change, require_auth() will never see X-MS-CLIENT-PRINCIPAL in
production, making all write endpoints permanently return 401. This plan wires the platform-
level auth validation that Plans 04-01 and 04-02 depend on for live operation.

Output:
- C:\Users\Sriram\azure-infrastructure\modules\postsapi.bicep (extended — 3 params + authsettingsV2)
- C:\Users\Sriram\azure-infrastructure\main.bicep (extended — 3 new params wired to postsapi module)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\Sriram\my-website\.planning\ROADMAP.md
@C:\Users\Sriram\my-website\.planning\phases\04-write-api\04-CONTEXT.md
@C:\Users\Sriram\my-website\.planning\phases\04-write-api\04-RESEARCH.md
@C:\Users\Sriram\my-website\.planning\phases\04-write-api\04-PATTERNS.md

<interfaces>
<!-- Bicep contracts from existing codebase. -->

From C:\Users\Sriram\azure-infrastructure\modules\postsapi.bicep (current state — lines 1–2):
  param location string
  param environment string = 'prod'
  Key resource: resource functionApp 'Microsoft.Web/sites@2022-09-01' (lines 46–99)
  functionApp.siteConfig.appSettings array ends at line 90 (POSTS_CONTAINER_NAME is last item)
  No authsettingsV2 resource exists yet.

From C:\Users\Sriram\azure-infrastructure\modules\ideasapi.bicep (analog for authsettingsV2):
  New params style: @description(...) param azureTenantId string (no default — required)
  Secure params style: @secure() @description(...) param ideasApiClientSecret string
  authsettingsV2 resource: parent: functionApp; name: 'authsettingsV2'
  CRITICAL DIFFERENCE from ideas-api: requireAuthentication: false + AllowAnonymous
    (ideas-api uses requireAuthentication: true + Return401 — do NOT copy that setting)
  openIdIssuer pattern: '${az.environment().authentication.loginEndpoint}${azureTenantId}/v2.0'
  allowedAudiences: ['api://${postsApiClientId}']

From C:\Users\Sriram\azure-infrastructure\main.bicep (postsapi module call — lines 176–183):
  module postsAPI 'modules/postsapi.bicep' = {
    name: 'postsAPIDeployment'
    scope: rg
    params: {
      location: location
      environment: environment
    }
  }
  — needs azureTenantId, postsApiClientId, postsApiClientSecret added to params block
  — azureTenantId already exists as a top-level param in main.bicep (line 34); reuse it
  — ideasApiClientSecret pattern (lines 46–48) shows how to declare secure params
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Easy Auth params and authsettingsV2 to postsapi.bicep</name>
  <files>
    C:\Users\Sriram\azure-infrastructure\modules\postsapi.bicep
  </files>
  <read_first>
    - C:\Users\Sriram\azure-infrastructure\modules\postsapi.bicep — read full file before modifying; identify exact insertion points for params, app setting, and authsettingsV2 resource
    - C:\Users\Sriram\azure-infrastructure\modules\ideasapi.bicep — read the authsettingsV2 resource block and param declarations as the analog; note the requireAuthentication difference
  </read_first>
  <action>
    Modify C:\Users\Sriram\azure-infrastructure\modules\postsapi.bicep with three additions.
    No existing lines are removed or reordered.

    Addition 1 — Three new params after line 2 (after "param environment string = 'prod'"):
      @description('Azure Entra ID tenant ID for EasyAuth')
      param azureTenantId string

      @description('posts-api App Registration client ID')
      param postsApiClientId string

      @secure()
      @description('posts-api App Registration client secret')
      param postsApiClientSecret string

    Addition 2 — New app setting inside functionApp siteConfig.appSettings array (after the
    POSTS_CONTAINER_NAME entry, before the closing bracket of the appSettings array):
      {
        name: 'POSTS_CLIENT_SECRET'
        value: postsApiClientSecret
      }

    Addition 3 — authsettingsV2 resource AFTER the functionApp resource block and BEFORE the
    blobDataContributorRole resource (insert between line 99 and 101 in the original file).
    The resource MUST use:
      parent: functionApp
      name: 'authsettingsV2'
      globalValidation.requireAuthentication: false           -- NOT true
      globalValidation.unauthenticatedClientAction: 'AllowAnonymous'  -- NOT 'Return401'
      identityProviders.azureActiveDirectory.enabled: true
      registration.clientId: postsApiClientId
      registration.clientSecretSettingName: 'POSTS_CLIENT_SECRET'
      registration.openIdIssuer: '${az.environment().authentication.loginEndpoint}${azureTenantId}/v2.0'
      validation.allowedAudiences: ['api://${postsApiClientId}']
      login.tokenStore.enabled: true

    WARNING: The ideas-api analog uses requireAuthentication: true + Return401 because all routes
    require auth. Posts-api must use false + AllowAnonymous to keep GET /api/posts public.
    Copying the ideas-api setting verbatim would break the public reading UI.
  </action>
  <verify>
    <automated>az bicep build --file C:\Users\Sriram\azure-infrastructure\modules\postsapi.bicep 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - az bicep build exits with code 0 (no errors)
    - postsapi.bicep contains "authsettingsV2" string
    - postsapi.bicep contains "requireAuthentication: false"
    - postsapi.bicep contains "AllowAnonymous"
    - postsapi.bicep contains "POSTS_CLIENT_SECRET"
    - postsapi.bicep contains "param postsApiClientId string"
    - postsapi.bicep does NOT contain "requireAuthentication: true" (would block public reads)
  </acceptance_criteria>
  <done>postsapi.bicep builds cleanly with authsettingsV2 resource and three new params</done>
</task>

<task type="auto">
  <name>Task 2: Wire new postsapi params in main.bicep</name>
  <files>
    C:\Users\Sriram\azure-infrastructure\main.bicep
  </files>
  <read_first>
    - C:\Users\Sriram\azure-infrastructure\main.bicep — read lines 30–55 (top-level params) and lines 175–185 (postsapi module call) to understand current structure before modifying
  </read_first>
  <action>
    Modify C:\Users\Sriram\azure-infrastructure\main.bicep with two additions.

    Addition 1 — Two new top-level params (after the existing ideasApiClientSecret param block,
    around lines 46–48). The azureTenantId param already exists at line 34 — do NOT add a duplicate.
    Add only:
      @description('posts-api App Registration client ID')
      param postsApiClientId string

      @secure()
      @description('posts-api App Registration client secret')
      param postsApiClientSecret string

    Addition 2 — Update the postsapi module call (lines 176–183) to pass the three new params:
      module postsAPI 'modules/postsapi.bicep' = {
        name: 'postsAPIDeployment'
        scope: rg
        params: {
          location: location
          environment: environment
          azureTenantId: azureTenantId        // already exists as top-level param
          postsApiClientId: postsApiClientId  // new
          postsApiClientSecret: postsApiClientSecret  // new
        }
      }

    No other module calls or params are changed.
  </action>
  <verify>
    <automated>az bicep build --file C:\Users\Sriram\azure-infrastructure\main.bicep 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - az bicep build exits with code 0 on main.bicep (no errors)
    - main.bicep contains "postsApiClientId: postsApiClientId" inside the postsAPI module params block
    - main.bicep contains "param postsApiClientId string" as a top-level declaration
    - main.bicep does NOT have two "param azureTenantId" declarations (no duplicate)
    - Both postsapi.bicep and main.bicep build cleanly together: az bicep build --file C:\Users\Sriram\azure-infrastructure\main.bicep
  </acceptance_criteria>
  <done>main.bicep builds cleanly; postsapi module receives all three Easy Auth params</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 04-01 and 04-02 implemented the three write handlers (create_post, update_post,
    delete_post) in posts-api. Plans 04-01/04-02 tests are all green. This task updates
    postsapi.bicep and main.bicep with Easy Auth configuration (authsettingsV2). The Bicep
    files build cleanly. Now the infrastructure must be deployed and the live endpoints verified.
  </what-built>
  <how-to-verify>
    Before running the deploy command, complete the required human setup steps:

    STEP 0 — Human prerequisite (one-time):
      a. Open Azure Portal → Microsoft Entra ID → App registrations → New registration
         Name: posts-api | Single tenant | No redirect URI | Register
      b. Note the Application (client) ID → this is postsApiClientId
      c. Go to Expose an API → Add a scope → accept default Application ID URI (api://<clientId>) → Save
      d. Go to Certificates & secrets → New client secret → any name → 24 months → Add
         Copy the Value immediately → this is postsApiClientSecret
      e. azureTenantId is already known (same tenant as ideas-api, found in Azure Portal → Entra ID → Overview)

    STEP 1 — Deploy Bicep (scoped to resource group, not full subscription):
      az deployment group create \
        --resource-group my-website-prod-rg \
        --template-file C:\Users\Sriram\azure-infrastructure\modules\postsapi.bicep \
        --parameters location=eastus environment=prod \
                     azureTenantId=<your-tenant-id> \
                     postsApiClientId=<client-id-from-step-0b> \
                     postsApiClientSecret=<secret-from-step-0d>

    STEP 2 — Verify public read still works:
      curl https://<posts-api-hostname>/api/posts
      Expected: 200 with {"posts": [...]} — NOT a 401

    STEP 3 — Verify write endpoint rejects unauthenticated request:
      curl -X POST https://<posts-api-hostname>/api/posts \
           -H "Content-Type: application/json" \
           -d '{"title":"test","description":"test","body":"test","published":false}'
      Expected: 401 {"error": "Unauthorized"}

    STEP 4 — Check Azure Portal → posts-api Function App → Authentication
      Confirm "Microsoft" identity provider appears with status "Enabled"
  </how-to-verify>
  <resume-signal>
    Type "verified" if all four steps pass.
    Or describe any failure (e.g., "GET /api/posts returns 401" → authsettingsV2 requireAuthentication
    setting needs correction; "deploy failed invalid client ID" → App Registration not completed).
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Azure Platform (Easy Auth) | Platform validates Bearer token before request reaches any handler |
| Bicep deploy pipeline → Azure Resource Manager | Bicep template deploys authsettingsV2 config via ARM API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Elevation of Privilege | authsettingsV2 requireAuthentication setting | mitigate | Must be false + AllowAnonymous (NOT true + Return401) — false lets platform inject X-MS-CLIENT-PRINCIPAL for authenticated requests while keeping GET routes public; handler enforces auth for writes |
| T-04-12 | Info Disclosure | postsApiClientSecret in Bicep | mitigate | Declared @secure() in both postsapi.bicep and main.bicep — ARM does not log secure param values; stored as encrypted app setting POSTS_CLIENT_SECRET on Function App |
| T-04-13 | Spoofing | CORS — unauthorized origin accessing write endpoints | mitigate | postsapi.bicep cors.allowedOrigins: ['https://www.quixotry.me'] already in place (line 91–95); platform handles OPTIONS preflight for POST/PUT/DELETE; no manual OPTIONS handler needed |
| T-04-14 | Tampering | App Registration client ID reuse | accept | Each API gets its own App Registration; posts-api client ID is separate from ideas-api (bb744b67) — audience validation in authsettingsV2 prevents cross-service token acceptance |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; Bicep-only changes |
</threat_model>

<verification>
After human-verify checkpoint passes:

1. GET /api/posts → 200 (public read unchanged)
2. POST /api/posts with no auth → 401 {"error": "Unauthorized"}
3. Azure Portal → posts-api Function App → Authentication shows Microsoft provider enabled
4. Full pytest suite still green (no regression from infra changes):
   cd C:\Users\Sriram\posts-api && .venv\Scripts\pytest -x -q
</verification>

<success_criteria>
- postsapi.bicep has authsettingsV2 resource with requireAuthentication: false + AllowAnonymous
- main.bicep passes azureTenantId, postsApiClientId, postsApiClientSecret to postsapi module
- Both Bicep files build without errors (az bicep build)
- After deploy: GET /api/posts returns 200 (not 401)
- After deploy: POST /api/posts with no Bearer token returns 401
- Azure Portal confirms Easy Auth provider is enabled on posts-api Function App
</success_criteria>

<output>
Create .planning/phases/04-write-api/04-03-SUMMARY.md when done
</output>
