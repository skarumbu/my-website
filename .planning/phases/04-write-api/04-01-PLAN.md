---
phase: 04-write-api
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - C:\Users\Sriram\posts-api\auth.py
  - C:\Users\Sriram\posts-api\function_app.py
  - C:\Users\Sriram\posts-api\tests\test_function_app.py
autonomous: true
requirements:
  - API-03
  - SEC-02

must_haves:
  truths:
    - "POST /api/posts with a valid X-MS-CLIENT-PRINCIPAL header creates a blob and returns 201 {slug}"
    - "POST /api/posts without X-MS-CLIENT-PRINCIPAL returns 401 {error: Unauthorized} and writes nothing"
    - "POST /api/posts with missing title or description returns 400 before touching storage"
    - "require_auth() raises ValueError when X-MS-CLIENT-PRINCIPAL header is absent"
    - "All 8+ tests for create_post and auth pass with pytest"
  artifacts:
    - path: "C:\\Users\\Sriram\\posts-api\\auth.py"
      provides: "require_auth() — decodes X-MS-CLIENT-PRINCIPAL, raises ValueError if absent"
      contains: "def require_auth"
    - path: "C:\\Users\\Sriram\\posts-api\\function_app.py"
      provides: "_unauthorized() helper + create_post handler + required imports"
      contains: "def create_post"
    - path: "C:\\Users\\Sriram\\posts-api\\tests\\test_function_app.py"
      provides: "Wave 0 stubs + unit + integration tests for create_post and auth"
      contains: "test_create_post_requires_auth"
  key_links:
    - from: "function_app.py::create_post"
      to: "auth.py::require_auth"
      via: "from auth import require_auth"
      pattern: "from auth import require_auth"
    - from: "function_app.py::create_post"
      to: "schema.py::build_post, validate_post, serialize_post"
      via: "from schema import build_post, validate_post, serialize_post"
      pattern: "from schema import"
    - from: "function_app.py::create_post"
      to: "slugs.py::generate_slug"
      via: "from slugs import generate_slug"
      pattern: "from slugs import generate_slug"
---

<objective>
Deliver the first vertical slice of the Write API: the `POST /api/posts` create endpoint,
protected by Easy Auth token validation, with full unit and integration test coverage.

Purpose: Establishes auth.py (the security foundation for all write operations) and the
complete create-post flow — auth gate → body parse → validate → slug → blob write → 201.
A caller with a valid Bearer token can create a post that immediately appears in storage.

Output:
- C:\Users\Sriram\posts-api\auth.py (new — verbatim copy of ideas-api/auth.py)
- C:\Users\Sriram\posts-api\function_app.py (extended — _unauthorized helper + create_post)
- C:\Users\Sriram\posts-api\tests\test_function_app.py (extended — Wave 0 stubs + 8+ new tests)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@C:\Users\Sriram\my-website\.planning\ROADMAP.md
@C:\Users\Sriram\my-website\.planning\phases\04-write-api\04-CONTEXT.md
@C:\Users\Sriram\my-website\.planning\phases\04-write-api\04-RESEARCH.md
@C:\Users\Sriram\my-website\.planning\phases\04-write-api\04-PATTERNS.md
@C:\Users\Sriram\my-website\.planning\phases\04-write-api\04-VALIDATION.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From C:\Users\Sriram\posts-api\function_app.py (current state — lines 1–16):
  Existing imports: json, os, re, azure.functions as func, ResourceNotFoundError
  Existing imports from local modules: from schema import parse_post; from slugs import get_container_client
  Constants: ALLOWED_ORIGIN = "https://www.quixotry.me"; SLUG_RE = re.compile(r"^[a-z0-9-]+$")
  _json_response(data: dict, status_code: int = 200) -> func.HttpResponse
    — always sets Access-Control-Allow-Origin: ALLOWED_ORIGIN
  Existing routes (MUST NOT be changed): health GET, list_posts GET /posts, get_post GET /posts/{slug}

From C:\Users\Sriram\ideas-api\auth.py (complete file — verbatim copy target):
  def require_auth(req: func.HttpRequest) -> tuple[str, str, str]
    — reads req.headers.get("X-MS-CLIENT-PRINCIPAL")
    — raises ValueError("Unauthenticated") if header absent
    — decodes: base64.b64decode(principal_b64 + "==")  ← the + "==" is load-bearing
    — returns (oid, email, name)

From C:\Users\Sriram\posts-api\schema.py (signatures needed):
  build_post(title, slug, date, description, body, published, updated_at=None) -> frontmatter.Post
  validate_post(post) -> list[str]  (empty list = valid)
  serialize_post(post) -> str  (YAML frontmatter + markdown body)

From C:\Users\Sriram\posts-api\slugs.py (signatures needed):
  generate_slug(title: str, client: ContainerClient) -> str
  get_container_client() -> ContainerClient

From C:\Users\Sriram\posts-api\tests\test_function_app.py (test style — lines 26–37, 49–59):
  Uses func.HttpRequest(method, body, url, params, route_params={}, headers={})
  Monkeypatches "function_app.get_container_client" with lambda: mock_client
  MagicMock() for mock_client; mock_client.get_blob_client.return_value = mock_blob
  Integration tests use container_client fixture from conftest.py
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create auth.py and Wave 0 test stubs</name>
  <files>
    C:\Users\Sriram\posts-api\auth.py
    C:\Users\Sriram\posts-api\tests\test_function_app.py
  </files>
  <read_first>
    - C:\Users\Sriram\ideas-api\auth.py — source for verbatim copy; read the entire file before writing
    - C:\Users\Sriram\posts-api\tests\test_function_app.py — read full file to understand existing test structure before appending
    - C:\Users\Sriram\posts-api\tests\conftest.py — confirm container_client fixture name and signature
  </read_first>
  <behavior>
    Wave 0 test stubs (all must fail with ImportError or AttributeError before implementation):
    - test_create_post_requires_auth: POST without X-MS-CLIENT-PRINCIPAL → assert status_code == 401
    - test_create_post_missing_title: POST with auth but no title → assert status_code == 400
    - test_create_post_missing_description: POST with auth but no description → assert status_code == 400
    - test_create_post_invalid_json: POST with auth but body=b"not-json" → assert status_code == 400
    - test_create_post_success (unit): POST with auth + valid body, monkeypatched storage → assert status_code == 201 and "slug" in body
    - test_create_post_integration: POST with auth + valid body, real Azurite container → assert status_code == 201 and slug blob exists in container

    Auth helper (add once near top of new write-tests section):
    - _make_auth_header(email="test@example.com") -> dict
      builds {"X-MS-CLIENT-PRINCIPAL": base64-encoded JSON claims}
      claims list: preferred_username, objectidentifier (oid), name
  </behavior>
  <action>
    1. Create C:\Users\Sriram\posts-api\auth.py as a verbatim copy of C:\Users\Sriram\ideas-api\auth.py.
       The file has exactly three top-level imports (base64, json, azure.functions as func) and one function
       require_auth(req: func.HttpRequest) -> tuple[str, str, str]. Do NOT modify any line — especially
       keep the base64.b64decode(principal_b64 + "==") padding exactly as-is.

    2. Append to C:\Users\Sriram\posts-api\tests\test_function_app.py a new section separated by a comment
       block "# ---------------------------------------------------------------------------\n# Write handler tests (Phase 4: API-03, API-04, API-05, SEC-02)\n# ---------------------------------------------------------------------------"

       Add the _make_auth_header() helper immediately after the section comment. It must use base64 and json
       (_json aliased to avoid shadowing the existing json import) to build the X-MS-CLIENT-PRINCIPAL header.

       Add all six Wave 0 stubs described in the behavior block. At this stage the stubs should call the
       handler functions (function_app.create_post etc.) even though they don't exist yet — this makes
       the stubs fail RED as required by TDD Wave 0. Use the same func.HttpRequest construction style as
       existing tests (method, body, url, params, headers kwargs).

       For the integration test stub, use the container_client fixture (same as existing integration tests
       at lines 86–116) and monkeypatch "function_app.get_container_client".
  </action>
  <verify>
    <automated>cd C:\Users\Sriram\posts-api && .venv\Scripts\pytest tests\test_function_app.py -k "create_post or requires_auth" -x -q 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - C:\Users\Sriram\posts-api\auth.py exists and contains exactly: "def require_auth", "X-MS-CLIENT-PRINCIPAL", "base64.b64decode(principal_b64 + \"==\")"
    - pytest -k "create_post or requires_auth" fails with AttributeError (function_app has no attribute create_post) — confirms stubs are wired correctly and RED
    - _make_auth_header helper exists in test file and builds a dict with key "X-MS-CLIENT-PRINCIPAL"
    - No existing tests (list_posts, get_post) are broken: pytest -k "not (create_post or requires_auth)" passes green
  </acceptance_criteria>
  <done>auth.py created verbatim from ideas-api; 6 Wave 0 test stubs RED; existing tests still green</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement create_post handler and turn stubs GREEN</name>
  <files>
    C:\Users\Sriram\posts-api\function_app.py
  </files>
  <read_first>
    - C:\Users\Sriram\posts-api\function_app.py — read full file before modifying; identify exact insertion points for imports and new functions
    - C:\Users\Sriram\posts-api\auth.py — confirm require_auth signature after Task 1
    - C:\Users\Sriram\posts-api\schema.py — confirm build_post, validate_post, serialize_post signatures
    - C:\Users\Sriram\posts-api\slugs.py — confirm generate_slug signature
    - C:\Users\Sriram\my-website\.planning\phases\04-write-api\04-PATTERNS.md — Pattern 1 and Pattern 2 (handler structure and _unauthorized helper)
  </read_first>
  <behavior>
    create_post handler behavior:
    - No X-MS-CLIENT-PRINCIPAL header → 401 {"error": "Unauthorized"} — no body parsing, no storage call
    - Body not valid JSON → 400 {"error": "Invalid JSON body"}
    - title missing or empty after strip() → 400 {"error": "title and description are required"}
    - description missing or empty after strip() → 400 {"error": "title and description are required"}
    - Valid auth + valid body → generate_slug, build_post, validate_post, serialize_post, upload_blob → 201 {"slug": slug}
    - Storage exception → 500 {"error": "storage error"}
  </behavior>
  <action>
    Modify C:\Users\Sriram\posts-api\function_app.py with two additions. No existing code is removed or changed.

    Addition 1 — New imports at top of file (add after existing import block, before the app = ... line):
      from auth import require_auth
      from schema import build_post, validate_post, serialize_post
      from slugs import generate_slug
      from datetime import datetime, timezone

    Addition 2 — _unauthorized() helper: add immediately after the existing _json_response() function
    (after line 25 in the current file). Use ALLOWED_ORIGIN constant (NOT the literal string "*").
    Signature: def _unauthorized(message: str = "Unauthorized") -> func.HttpResponse

    Addition 3 — create_post handler: add as a new function after the _unauthorized helper.
    Decorator: @app.route(route="posts", methods=["POST"])
    The handler is a SEPARATE function from the existing list_posts — do NOT modify list_posts.

    Handler call sequence (must follow this order exactly, per D-05/D-06 and PATTERNS.md Pattern 1):
      1. try: require_auth(req) / except ValueError: return _unauthorized()
      2. try: body = req.get_json() / except Exception: return 400 Invalid JSON body
      3. Extract title, description, post_body, published from body with .get() + .strip() defaults
      4. if not title or not description: return 400 "title and description are required"
      5. try: client = get_container_client(); slug = generate_slug(title, client)
         post = build_post(title=title, slug=slug, date=datetime.now(timezone.utc).isoformat(),
                           description=description, body=post_body, published=bool(published))
         errors = validate_post(post); if errors: return 400 errors[0]
         content = serialize_post(post)
         client.get_blob_client(f"{slug}.md").upload_blob(content.encode(), overwrite=True)
         return _json_response({"slug": slug}, status_code=201)
      6. except Exception: return 500 "storage error"

    Note: body.get("body", "").strip() — use "body" as the key, not "content"; published defaults to False.
  </action>
  <verify>
    <automated>cd C:\Users\Sriram\posts-api && .venv\Scripts\pytest tests\test_function_app.py -k "create_post or requires_auth" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - pytest -k "create_post or requires_auth" passes all stubs GREEN (no failures)
    - grep result: function_app.py contains "from auth import require_auth"
    - grep result: function_app.py contains "def _unauthorized"
    - grep result: function_app.py contains "def create_post"
    - grep result: function_app.py contains "@app.route(route=\"posts\", methods=[\"POST\"])"
    - _unauthorized() uses ALLOWED_ORIGIN constant — grep "ALLOWED_ORIGIN" inside def _unauthorized confirms it
    - Full suite still passes: .venv\Scripts\pytest -x -q (all pre-existing tests green)
  </acceptance_criteria>
  <done>create_post handler implemented; all 6 create_post/auth stubs pass; full suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Azure Function | HTTP request crosses here; Bearer token injected by Easy Auth platform before handler fires |
| Handler → Blob Storage | Uses DefaultAzureCredential (managed identity); no credentials in handler code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | create_post handler | mitigate | require_auth(req) is the FIRST call in handler body before any body parsing or storage access; ValueError → _unauthorized() 401 per D-05/D-06 |
| T-04-02 | Tampering | POST body — YAML injection via title | mitigate | serialize_post() uses python-frontmatter (frontmatter.dumps); never f-string YAML; enforced by schema.py |
| T-04-03 | Spoofing | CORS — non-allowlisted origin calling POST | mitigate | ALLOWED_ORIGIN = "https://www.quixotry.me" in _unauthorized() and _json_response(); platform CORS config in postsapi.bicep (Plan 04-03) |
| T-04-04 | Tampering | Input validation bypass — missing required fields | mitigate | validate after strip(): if not title or not description → 400 before build_post is called |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed this phase; all dependencies already vetted in Phase 1/2 |
</threat_model>

<verification>
Run after both tasks complete:

1. Full test suite green: cd C:\Users\Sriram\posts-api && .venv\Scripts\pytest -x -q
2. Auth gate confirmed: pytest -k "requires_auth" -v — all auth tests pass
3. Create post success: pytest -k "create_post_success or create_post_integration" -v
4. No pre-existing test regressions: pytest -k "list_posts or get_post" -v
</verification>

<success_criteria>
- auth.py exists with require_auth() identical to ideas-api/auth.py
- create_post handler exists at @app.route(route="posts", methods=["POST"])
- POST without X-MS-CLIENT-PRINCIPAL → 401 {"error": "Unauthorized"}
- POST with valid auth + {title, description, body, published} → 201 {"slug": "..."}
- POST with missing title or description → 400 {"error": "title and description are required"}
- All pytest tests pass (full suite green)
- No changes to list_posts, get_post, health handlers
</success_criteria>

<output>
Create .planning/phases/04-write-api/04-01-SUMMARY.md when done
</output>
