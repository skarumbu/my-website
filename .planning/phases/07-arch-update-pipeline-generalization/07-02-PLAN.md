---
plan: "07-02"
title: "Existing 7 repo workflow cleanup"
phase: 7
wave: 1
depends_on: []
autonomous: true
files_modified:
  - C:/Users/Sriram/digits/.github/workflows/deploy.yml
  - C:/Users/Sriram/dashboard_api/.github/workflows/deploy.yml
  - C:/Users/Sriram/momentum_finder/.github/workflows/deploy.yml
  - C:/Users/Sriram/trail_finder/.github/workflows/deploy.yml
  - C:/Users/Sriram/ideas-api/.github/workflows/deploy.yml
  - C:/Users/Sriram/ideas-bot/.github/workflows/deploy.yml
  - C:/Users/Sriram/learning-plan-api/.github/workflows/deploy.yml
requirements:
  - D-08
  - D-10
  - D-13
  - D-14
  - D-15
must_haves:
  truths:
    - "D-08: No deploy.yml in any of the 7 repos contains an update-architecture job"
    - "D-10: In each repo, update-arch-content.needs points to the correct deploy job name (not update-architecture)"
    - "D-13/D-14: digits, dashboard_api, momentum_finder, trail_finder update-arch-content jobs reference DESIGN_DOC_GH_TOKEN (not MY_WEBSITE_DISPATCH_TOKEN)"
    - "D-15: No other secret references are changed — only the ARCH_UPDATE_GH_TOKEN mapping inside update-arch-content is updated"
  tests: []
risks:
  - "momentum_finder and trail_finder use 'build-and-deploy' as the deploy job name — must NOT use 'needs: deploy'"
  - "ideas-bot uses 'build-and-deploy' as the deploy job name — must NOT use 'needs: deploy'"
  - "ideas-api, learning-plan-api already use DESIGN_DOC_GH_TOKEN — verify before editing to avoid double-change"
  - "MY_WEBSITE_DISPATCH_TOKEN may still exist in these repos for other purposes (D-15) — only the ARCH_UPDATE_GH_TOKEN line in update-arch-content is touched"
---

## Objective

Remove the dead `update-architecture` job from each of the 7 existing package repos (D-08, D-10) and update the surviving `update-arch-content` job's `needs:` to point directly to the deploy job (D-10). Where required, rename the secret reference from `MY_WEBSITE_DISPATCH_TOKEN` to `DESIGN_DOC_GH_TOKEN` (D-13, D-14).

These are pure YAML edits — no code changes, no new files. Each repo is independent; all 7 can be edited in parallel.

The reusable workflow (`arch-content-update-reusable.yml`) in my-website is NOT modified — it is already correct.

## Reference: What the surviving update-arch-content block looks like after editing

For repos with deploy job named `deploy`:
```yaml
  update-arch-content:
    needs: deploy
    uses: skarumbu/my-website/.github/workflows/arch-content-update-reusable.yml@main
    with:
      repo_name: <key>
      commit_sha: ${{ github.sha }}
      commit_message: ${{ github.event.head_commit.message }}
    secrets:
      ARCH_CONTENT_FOUNDRY_KEY: ${{ secrets.ARCH_CONTENT_FOUNDRY_KEY }}
      ARCH_UPDATE_GH_TOKEN: ${{ secrets.DESIGN_DOC_GH_TOKEN }}
```

For repos with deploy job named `build-and-deploy`:
```yaml
  update-arch-content:
    needs: build-and-deploy
    uses: skarumbu/my-website/.github/workflows/arch-content-update-reusable.yml@main
    with:
      repo_name: <key>
      commit_sha: ${{ github.sha }}
      commit_message: ${{ github.event.head_commit.message }}
    secrets:
      ARCH_CONTENT_FOUNDRY_KEY: ${{ secrets.ARCH_CONTENT_FOUNDRY_KEY }}
      ARCH_UPDATE_GH_TOKEN: ${{ secrets.DESIGN_DOC_GH_TOKEN }}
```

## Tasks

### Task 1: digits — remove update-architecture job, fix needs chain, rename secret (D-08, D-10, D-14)

**File:** `C:/Users/Sriram/digits/.github/workflows/deploy.yml`

**Changes:**

1. Delete the entire `update-architecture` job block (lines 36–61 per RESEARCH.md). This is the block that starts with `update-architecture:` and ends before `update-arch-content:`. It begins with `needs: deploy` and contains the `Checkout my-website` and `Update deploy metadata` steps. Delete from the `update-architecture:` job name line through the last line of its steps (the `git push` line).

2. In the surviving `update-arch-content` job:
   - Change `needs: update-architecture` to `needs: deploy`
   - Change `ARCH_UPDATE_GH_TOKEN: ${{ secrets.MY_WEBSITE_DISPATCH_TOKEN }}` to `ARCH_UPDATE_GH_TOKEN: ${{ secrets.DESIGN_DOC_GH_TOKEN }}`

   The `repo_name`, `commit_sha`, `commit_message`, and `ARCH_CONTENT_FOUNDRY_KEY` lines are NOT changed.

**Deploy job name for digits:** `deploy`
**repo_name value (do not change):** `digits`
**Secret before:** `MY_WEBSITE_DISPATCH_TOKEN`
**Secret after:** `DESIGN_DOC_GH_TOKEN`

**Verification:** `grep -n "update-architecture" C:/Users/Sriram/digits/.github/workflows/deploy.yml` must return no output. `grep -n "MY_WEBSITE_DISPATCH_TOKEN" C:/Users/Sriram/digits/.github/workflows/deploy.yml` must return no output. `grep -n "needs:" C:/Users/Sriram/digits/.github/workflows/deploy.yml` must show `update-arch-content` depending on `deploy`.

**Done:** digits deploy.yml contains no `update-architecture` job; `update-arch-content` has `needs: deploy` and `ARCH_UPDATE_GH_TOKEN: ${{ secrets.DESIGN_DOC_GH_TOKEN }}`.

---

### Task 2: dashboard_api — remove update-architecture job, fix needs chain, rename secret (D-08, D-10, D-14)

**File:** `C:/Users/Sriram/dashboard_api/.github/workflows/deploy.yml`

**Changes:** Identical pattern to digits.

1. Delete the entire `update-architecture` job block (same structure as digits — `Checkout my-website` + `Update deploy metadata` steps with jq writing to architecture-metadata.json).

2. In the surviving `update-arch-content` job:
   - Change `needs: update-architecture` to `needs: deploy`
   - Change `ARCH_UPDATE_GH_TOKEN: ${{ secrets.MY_WEBSITE_DISPATCH_TOKEN }}` to `ARCH_UPDATE_GH_TOKEN: ${{ secrets.DESIGN_DOC_GH_TOKEN }}`

**Deploy job name for dashboard_api:** `deploy`
**repo_name value (do not change):** `dashboard-api`
**Secret before:** `MY_WEBSITE_DISPATCH_TOKEN`
**Secret after:** `DESIGN_DOC_GH_TOKEN`

**Verification:** `grep -n "update-architecture" C:/Users/Sriram/dashboard_api/.github/workflows/deploy.yml` must return no output. `grep -n "MY_WEBSITE_DISPATCH_TOKEN" C:/Users/Sriram/dashboard_api/.github/workflows/deploy.yml` must return no output.

**Done:** dashboard_api deploy.yml contains no `update-architecture` job; `update-arch-content` has `needs: deploy` and `ARCH_UPDATE_GH_TOKEN: ${{ secrets.DESIGN_DOC_GH_TOKEN }}`.

---

### Task 3: momentum_finder — remove update-architecture job, fix needs chain, rename secret (D-08, D-10, D-14)

**File:** `C:/Users/Sriram/momentum_finder/.github/workflows/deploy.yml`

**CRITICAL:** Deploy job name is `build-and-deploy`, NOT `deploy`. Do NOT use `needs: deploy`.

**Changes:**

1. Delete the entire `update-architecture` job block (same structure — checkout my-website + jq metadata update).

2. In the surviving `update-arch-content` job:
   - Change `needs: update-architecture` to `needs: build-and-deploy`
   - Change `ARCH_UPDATE_GH_TOKEN: ${{ secrets.MY_WEBSITE_DISPATCH_TOKEN }}` to `ARCH_UPDATE_GH_TOKEN: ${{ secrets.DESIGN_DOC_GH_TOKEN }}`

**Deploy job name for momentum_finder:** `build-and-deploy`
**repo_name value (do not change):** `momentum-finder`
**Secret before:** `MY_WEBSITE_DISPATCH_TOKEN`
**Secret after:** `DESIGN_DOC_GH_TOKEN`

**Verification:** `grep -n "update-architecture" C:/Users/Sriram/momentum_finder/.github/workflows/deploy.yml` must return no output. `grep -n "MY_WEBSITE_DISPATCH_TOKEN" C:/Users/Sriram/momentum_finder/.github/workflows/deploy.yml` must return no output. `grep -A2 "update-arch-content:" C:/Users/Sriram/momentum_finder/.github/workflows/deploy.yml` must show `needs: build-and-deploy`.

**Done:** momentum_finder deploy.yml contains no `update-architecture` job; `update-arch-content` has `needs: build-and-deploy` and `ARCH_UPDATE_GH_TOKEN: ${{ secrets.DESIGN_DOC_GH_TOKEN }}`.

---

### Task 4: trail_finder — remove update-architecture job, fix needs chain, rename secret (D-08, D-10, D-14)

**File:** `C:/Users/Sriram/trail_finder/.github/workflows/deploy.yml`

**CRITICAL:** Deploy job name is `build-and-deploy`, NOT `deploy`.

**Changes:** Identical pattern to momentum_finder.

1. Delete the entire `update-architecture` job block.

2. In the surviving `update-arch-content` job:
   - Change `needs: update-architecture` to `needs: build-and-deploy`
   - Change `ARCH_UPDATE_GH_TOKEN: ${{ secrets.MY_WEBSITE_DISPATCH_TOKEN }}` to `ARCH_UPDATE_GH_TOKEN: ${{ secrets.DESIGN_DOC_GH_TOKEN }}`

**Deploy job name for trail_finder:** `build-and-deploy`
**repo_name value (do not change):** `trail-finder`
**Secret before:** `MY_WEBSITE_DISPATCH_TOKEN`
**Secret after:** `DESIGN_DOC_GH_TOKEN`

**Verification:** `grep -n "update-architecture" C:/Users/Sriram/trail_finder/.github/workflows/deploy.yml` must return no output. `grep -n "MY_WEBSITE_DISPATCH_TOKEN" C:/Users/Sriram/trail_finder/.github/workflows/deploy.yml` must return no output. `grep -A2 "update-arch-content:" C:/Users/Sriram/trail_finder/.github/workflows/deploy.yml` must show `needs: build-and-deploy`.

**Done:** trail_finder deploy.yml contains no `update-architecture` job; `update-arch-content` has `needs: build-and-deploy` and `ARCH_UPDATE_GH_TOKEN: ${{ secrets.DESIGN_DOC_GH_TOKEN }}`.

---

### Task 5: ideas-api — remove update-architecture job, fix needs chain (D-08, D-10) — secret already correct

**File:** `C:/Users/Sriram/ideas-api/.github/workflows/deploy.yml`

**Secret status:** ideas-api already uses `DESIGN_DOC_GH_TOKEN` — do NOT change the secret reference.

**Changes:**

1. Delete the entire `update-architecture` job block.

2. In the surviving `update-arch-content` job:
   - Change `needs: update-architecture` to `needs: deploy`
   - The `ARCH_UPDATE_GH_TOKEN: ${{ secrets.DESIGN_DOC_GH_TOKEN }}` line stays as-is.

**Deploy job name for ideas-api:** `deploy`
**repo_name value (do not change):** `ideas-api`

**Verification:** `grep -n "update-architecture" C:/Users/Sriram/ideas-api/.github/workflows/deploy.yml` must return no output. `grep -A2 "update-arch-content:" C:/Users/Sriram/ideas-api/.github/workflows/deploy.yml` must show `needs: deploy`.

**Done:** ideas-api deploy.yml contains no `update-architecture` job; `update-arch-content` has `needs: deploy`; secret reference is unchanged (`DESIGN_DOC_GH_TOKEN`).

---

### Task 6: ideas-bot — remove update-architecture job, fix needs chain (D-08, D-10) — secret already correct

**File:** `C:/Users/Sriram/ideas-bot/.github/workflows/deploy.yml`

**CRITICAL:** Deploy job name is `build-and-deploy`, NOT `deploy`.
**Secret status:** ideas-bot already uses `DESIGN_DOC_GH_TOKEN` — do NOT change the secret reference.

**Changes:**

1. Delete the entire `update-architecture` job block.

2. In the surviving `update-arch-content` job:
   - Change `needs: update-architecture` to `needs: build-and-deploy`
   - The `ARCH_UPDATE_GH_TOKEN: ${{ secrets.DESIGN_DOC_GH_TOKEN }}` line stays as-is.

**Deploy job name for ideas-bot:** `build-and-deploy`
**repo_name value (do not change):** `ideas-bot`

**Verification:** `grep -n "update-architecture" C:/Users/Sriram/ideas-bot/.github/workflows/deploy.yml` must return no output. `grep -A2 "update-arch-content:" C:/Users/Sriram/ideas-bot/.github/workflows/deploy.yml` must show `needs: build-and-deploy`.

**Done:** ideas-bot deploy.yml contains no `update-architecture` job; `update-arch-content` has `needs: build-and-deploy`; secret reference is unchanged (`DESIGN_DOC_GH_TOKEN`).

---

### Task 7: learning-plan-api — remove update-architecture job, fix needs chain (D-08, D-10) — secret already correct

**File:** `C:/Users/Sriram/learning-plan-api/.github/workflows/deploy.yml`

**Secret status:** learning-plan-api already uses `DESIGN_DOC_GH_TOKEN` — do NOT change the secret reference.

**Changes:**

1. Delete the entire `update-architecture` job block.

2. In the surviving `update-arch-content` job:
   - Change `needs: update-architecture` to `needs: deploy`
   - The `ARCH_UPDATE_GH_TOKEN: ${{ secrets.DESIGN_DOC_GH_TOKEN }}` line stays as-is.

**Deploy job name for learning-plan-api:** `deploy`
**repo_name value (do not change):** `learning-plan-api`

**Verification:** `grep -n "update-architecture" C:/Users/Sriram/learning-plan-api/.github/workflows/deploy.yml` must return no output. `grep -A2 "update-arch-content:" C:/Users/Sriram/learning-plan-api/.github/workflows/deploy.yml` must show `needs: deploy`.

**Done:** learning-plan-api deploy.yml contains no `update-architecture` job; `update-arch-content` has `needs: deploy`; secret reference is unchanged (`DESIGN_DOC_GH_TOKEN`).

---

## Verification

After all 7 tasks complete, run the following from any shell:

```
grep -rn "update-architecture" \
  C:/Users/Sriram/digits/.github/workflows/deploy.yml \
  C:/Users/Sriram/dashboard_api/.github/workflows/deploy.yml \
  C:/Users/Sriram/momentum_finder/.github/workflows/deploy.yml \
  C:/Users/Sriram/trail_finder/.github/workflows/deploy.yml \
  C:/Users/Sriram/ideas-api/.github/workflows/deploy.yml \
  C:/Users/Sriram/ideas-bot/.github/workflows/deploy.yml \
  C:/Users/Sriram/learning-plan-api/.github/workflows/deploy.yml
```
Must return no output (zero matches across all 7 files).

```
grep -rn "MY_WEBSITE_DISPATCH_TOKEN" \
  C:/Users/Sriram/digits/.github/workflows/deploy.yml \
  C:/Users/Sriram/dashboard_api/.github/workflows/deploy.yml \
  C:/Users/Sriram/momentum_finder/.github/workflows/deploy.yml \
  C:/Users/Sriram/trail_finder/.github/workflows/deploy.yml
```
Must return no output (zero matches in the 4 repos that had the old secret name).

## Success Criteria

- Zero `update-architecture` job references across all 7 deploy.yml files
- Zero `MY_WEBSITE_DISPATCH_TOKEN` references in digits, dashboard_api, momentum_finder, trail_finder deploy.yml files
- All 7 repos have `update-arch-content.needs` pointing to their correct deploy job name:
  - `deploy`: digits, dashboard_api, ideas-api, learning-plan-api
  - `build-and-deploy`: momentum_finder, trail_finder, ideas-bot
- All 7 repos have `ARCH_UPDATE_GH_TOKEN: ${{ secrets.DESIGN_DOC_GH_TOKEN }}`

## Output

Create `.planning/phases/07-arch-update-pipeline-generalization/07-02-SUMMARY.md` when done.
