---
plan: "07-03"
title: "New repos wiring — azure-infrastructure and posts-api"
phase: 7
wave: 2
depends_on:
  - "07-01"
autonomous: true
files_modified:
  - C:/Users/Sriram/azure-infrastructure/.github/workflows/deploy.yml
  - C:/Users/Sriram/azure-infrastructure/.github/workflows/dispatch-architecture-update.yml
  - C:/Users/Sriram/posts-api/.github/workflows/deploy.yml
requirements:
  - D-11
  - D-12
must_haves:
  truths:
    - "D-11: C:/Users/Sriram/azure-infrastructure/.github/workflows/dispatch-architecture-update.yml is deleted"
    - "D-11: azure-infrastructure/deploy.yml contains an update-arch-content job with needs: deploy calling the reusable workflow"
    - "D-12: posts-api/deploy.yml contains an update-arch-content job with needs: deploy calling the reusable workflow"
    - "D-12: posts-api update-arch-content job uses repo_name: posts-api (matches the key added to architecture-content.json in PLAN-01)"
    - "The reusable workflow is called identically in both repos: uses: skarumbu/my-website/.github/workflows/arch-content-update-reusable.yml@main"
    - "Both repos use ARCH_UPDATE_GH_TOKEN: ${{ secrets.DESIGN_DOC_GH_TOKEN }}"
  tests: []
risks:
  - "PLAN-01 must complete first — posts-api key in architecture-content.json must exist before posts-api's first deploy triggers the reusable workflow"
  - "azure-infrastructure triggers on 'master' branch (not 'main') — but no trigger config change is needed in deploy.yml since update-arch-content inherits the existing triggers"
  - "posts-api has a test job that gates deploy (chain: test -> deploy -> update-arch-content) — do NOT set needs: [test, deploy], only needs: deploy"
  - "azure-infrastructure already has a full entry in architecture-content.json — do NOT run register_package.py for it"
---

## Objective

Wire the two new repos into the reusable architecture content update pipeline (D-11, D-12):

1. **azure-infrastructure:** Delete the broken `dispatch-architecture-update.yml` workflow; add an `update-arch-content` job to `deploy.yml` after the existing `deploy` job.
2. **posts-api:** Add an `update-arch-content` job to `deploy.yml` after the existing `deploy` job.

This plan depends on PLAN-01 because the `posts-api` key in `architecture-content.json` must exist before the reusable workflow runs on a posts-api deploy. Wave 2.

## Pre-flight check

Before making any edits, read the current state of each file to confirm the exact job names and structure:

- `C:/Users/Sriram/azure-infrastructure/.github/workflows/deploy.yml` — confirm the deploy job name is `deploy` and identify where to append the new job
- `C:/Users/Sriram/posts-api/.github/workflows/deploy.yml` — confirm job names are `test` and `deploy`, and that the deploy job has no `update-arch-content` yet

This is mandatory because RESEARCH.md was written at research time; the files may have been updated since.

## Tasks

### Task 1: azure-infrastructure — delete dispatch-architecture-update.yml (D-11)

**File to delete:** `C:/Users/Sriram/azure-infrastructure/.github/workflows/dispatch-architecture-update.yml`

Delete this file entirely. It fires a `repository_dispatch` event and uses `MY_WEBSITE_DISPATCH_TOKEN`. After Phase 7, azure-infrastructure's deploy.yml will call the reusable workflow directly instead.

**Verification:** Confirm the file no longer exists: `ls C:/Users/Sriram/azure-infrastructure/.github/workflows/` must not list `dispatch-architecture-update.yml`.

**Done:** `dispatch-architecture-update.yml` does not exist in `C:/Users/Sriram/azure-infrastructure/.github/workflows/`.

---

### Task 2: azure-infrastructure — add update-arch-content job to deploy.yml (D-11)

**File modified:** `C:/Users/Sriram/azure-infrastructure/.github/workflows/deploy.yml`

**Context from RESEARCH.md:**
- Deploy job name: `deploy`
- Branch trigger: `master` (the existing deploy.yml triggers on `master` — this is correct and must not be changed)
- Last step in the `deploy` job is a Bicep deployment step (`az deployment sub create`)
- azure-infrastructure already has a full entry in `architecture-content.json` — do NOT add or modify any JSON entry
- `repo_name` input value: `azure-infrastructure` (must match the existing key in architecture-content.json)

**Action:** Append the following job at the end of the `jobs:` section in `deploy.yml`, after the existing `deploy` job. Maintain consistent YAML indentation (2 spaces for job-level keys):

```yaml
  update-arch-content:
    needs: deploy
    uses: skarumbu/my-website/.github/workflows/arch-content-update-reusable.yml@main
    with:
      repo_name: azure-infrastructure
      commit_sha: ${{ github.sha }}
      commit_message: ${{ github.event.head_commit.message }}
    secrets:
      ARCH_CONTENT_FOUNDRY_KEY: ${{ secrets.ARCH_CONTENT_FOUNDRY_KEY }}
      ARCH_UPDATE_GH_TOKEN: ${{ secrets.DESIGN_DOC_GH_TOKEN }}
```

Do not add any `if:` condition — the job should run on every push that triggers `deploy`.

**Verification:**
1. `grep -n "update-arch-content" C:/Users/Sriram/azure-infrastructure/.github/workflows/deploy.yml` must show the new job
2. `grep -A2 "update-arch-content:" C:/Users/Sriram/azure-infrastructure/.github/workflows/deploy.yml` must show `needs: deploy`
3. `grep -n "DESIGN_DOC_GH_TOKEN" C:/Users/Sriram/azure-infrastructure/.github/workflows/deploy.yml` must show the secret reference
4. YAML syntax check: `python -c "import yaml; yaml.safe_load(open('C:/Users/Sriram/azure-infrastructure/.github/workflows/deploy.yml').read()); print('valid YAML')"` must print `valid YAML`

**Done:** azure-infrastructure/deploy.yml has an `update-arch-content` job; `needs: deploy`; `repo_name: azure-infrastructure`; `ARCH_UPDATE_GH_TOKEN: ${{ secrets.DESIGN_DOC_GH_TOKEN }}`; file is valid YAML.

---

### Task 3: posts-api — add update-arch-content job to deploy.yml (D-12)

**File modified:** `C:/Users/Sriram/posts-api/.github/workflows/deploy.yml`

**Context from RESEARCH.md:**
- Job chain in deploy.yml: `test` (runs on PR + push) → `deploy` (push only, has `needs: test` and `if: github.event_name == 'push'`)
- Deploy job name: `deploy`
- Branch trigger: `main` (push only)
- `repo_name` input value: `posts-api` (must match the key added to architecture-content.json in PLAN-01 Task 3)

**Action:** Append the following job at the end of the `jobs:` section in `deploy.yml`, after the existing `deploy` job:

```yaml
  update-arch-content:
    needs: deploy
    uses: skarumbu/my-website/.github/workflows/arch-content-update-reusable.yml@main
    with:
      repo_name: posts-api
      commit_sha: ${{ github.sha }}
      commit_message: ${{ github.event.head_commit.message }}
    secrets:
      ARCH_CONTENT_FOUNDRY_KEY: ${{ secrets.ARCH_CONTENT_FOUNDRY_KEY }}
      ARCH_UPDATE_GH_TOKEN: ${{ secrets.DESIGN_DOC_GH_TOKEN }}
```

**CRITICAL — do NOT add `if: github.event_name == 'push'` to this job.** The `deploy` job already has this gate. GitHub Actions skips downstream jobs when an upstream job is skipped, so `update-arch-content` will automatically be skipped on PRs because `deploy` is skipped. Adding a duplicate `if:` is redundant and could break the dependency.

**CRITICAL — do NOT use `needs: [test, deploy]`.** The correct value is `needs: deploy` only. The `test → deploy` chain is already established; adding `test` here is redundant and error-prone.

**Verification:**
1. `grep -n "update-arch-content" C:/Users/Sriram/posts-api/.github/workflows/deploy.yml` must show the new job
2. `grep -A2 "update-arch-content:" C:/Users/Sriram/posts-api/.github/workflows/deploy.yml` must show `needs: deploy`
3. `grep -n "posts-api" C:/Users/Sriram/posts-api/.github/workflows/deploy.yml` must show `repo_name: posts-api`
4. `grep -n "DESIGN_DOC_GH_TOKEN" C:/Users/Sriram/posts-api/.github/workflows/deploy.yml` must show the secret reference
5. YAML syntax check: `python -c "import yaml; yaml.safe_load(open('C:/Users/Sriram/posts-api/.github/workflows/deploy.yml').read()); print('valid YAML')"` must print `valid YAML`

**Done:** posts-api/deploy.yml has an `update-arch-content` job; `needs: deploy`; `repo_name: posts-api`; `ARCH_UPDATE_GH_TOKEN: ${{ secrets.DESIGN_DOC_GH_TOKEN }}`; file is valid YAML.

---

## Verification

After all 3 tasks complete:

```
# dispatch workflow is gone
ls C:/Users/Sriram/azure-infrastructure/.github/workflows/ | grep dispatch

# Both new repos have the update-arch-content job
grep -n "update-arch-content" C:/Users/Sriram/azure-infrastructure/.github/workflows/deploy.yml
grep -n "update-arch-content" C:/Users/Sriram/posts-api/.github/workflows/deploy.yml

# Both use DESIGN_DOC_GH_TOKEN
grep -n "DESIGN_DOC_GH_TOKEN" C:/Users/Sriram/azure-infrastructure/.github/workflows/deploy.yml
grep -n "DESIGN_DOC_GH_TOKEN" C:/Users/Sriram/posts-api/.github/workflows/deploy.yml

# Both are valid YAML
python -c "import yaml; yaml.safe_load(open('C:/Users/Sriram/azure-infrastructure/.github/workflows/deploy.yml').read()); print('azure-infra: valid')"
python -c "import yaml; yaml.safe_load(open('C:/Users/Sriram/posts-api/.github/workflows/deploy.yml').read()); print('posts-api: valid')"
```

Expected output: `ls | grep dispatch` returns nothing; both grep calls return matches; both YAML checks print valid.

## Success Criteria

- `C:/Users/Sriram/azure-infrastructure/.github/workflows/dispatch-architecture-update.yml` does not exist
- azure-infrastructure/deploy.yml has `update-arch-content` job with `needs: deploy`, `repo_name: azure-infrastructure`, `ARCH_UPDATE_GH_TOKEN: ${{ secrets.DESIGN_DOC_GH_TOKEN }}`
- posts-api/deploy.yml has `update-arch-content` job with `needs: deploy`, `repo_name: posts-api`, `ARCH_UPDATE_GH_TOKEN: ${{ secrets.DESIGN_DOC_GH_TOKEN }}`
- Both files are valid YAML

## Output

Create `.planning/phases/07-arch-update-pipeline-generalization/07-03-SUMMARY.md` when done.
